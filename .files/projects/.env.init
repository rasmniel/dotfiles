# Root directory where projects live.
export PROJECTS_ROOT="$HOME/projects"
# Default git user for use when cloning.
export DEFAULT_GIT_USER="user"
# Default git remote for use when cloning.
export DEFAULT_GIT_REMOTE="git.com"
