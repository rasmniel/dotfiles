#!/usr/bin/env bash

# The service installation directory to deploy from.
export SOURCE_ROOT="$HOME/services"
# The destination directory for services to be deployed to.
export SERVICE_ROOT="/srv"
# The domain that services should live on. Required to setup Caddy.
export DOMAIN="localhost" # localhost is typically already mapped and doesn't need a manual host entry.

